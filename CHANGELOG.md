# Changelog

## Unreleased

- Made the game-client bridge per adapter (`app/config.mjs`'s `bridgeFor`/`bridgeQueueFor`/`bridgeStatusFor`, mirroring the existing per-adapter inbox): `POST /api/bridge`, `GET /api/bridge/status`, and `POST /api/setup/install`'s running-script guard used to be hard-coded to `<data>/bridge/tazuo/` regardless of which client was actually configured, so a Razor Enhanced player's fully-capable Highlight/Grab/Go-to buttons queued commands into a folder that adapter's own `packrat-bridge.py` never reads — a silent no-op. The bridge queue/status routes now resolve the directory live off `settings.client.adapter` (falling back to `tazuo` only when no client is configured, matching the previous behavior for that case); the install guard checks the adapter actually being installed, not necessarily the configured one. Neither adapter script needed to change — each already read/wrote its own `bridge/<its-own-id>/` directory. No data migration for an existing TazUO player: `bridgeFor("tazuo")` is byte-identical to the path the app always used. `docs/bridge-protocol.md`, `docs/architecture.md` and `CONTRIBUTING.md` updated to match.
- Fixed two edge cases in the ClassicUO web scanner's chunked print loop (`adapters/classicuo-web/packrat-scanner.ts`): a chunk boundary could split a UTF-16 surrogate pair (an astral character, e.g. an emoji in an engraved item name), corrupting it on any copy/paste path that doesn't preserve a lone surrogate byte-for-byte — `chunkEnd()` now backs the boundary off by one whenever it would land inside a pair. U+2028/U+2029 (LINE/PARAGRAPH SEPARATOR), unlike `\r`/`\n`, are valid *unescaped* characters inside a JSON string, so a console that treats either as a line break on render or copy could turn it into a real newline that the receiving side's own newline-stripping would then delete — both are now escaped explicitly before printing. Also added a real parse check for `packrat-scanner.ts` (`app/classicuo-web-adapter.test.mjs` now spawns Node's own TypeScript type-stripping directly against the file) after a source-corruption bug — a literal `\u` escape sequence typed next to raw hex digits silently became the actual separator character during an editing pass — shipped through every existing test undetected, since none of them had ever actually parsed the file as code.

- Added the Import tab (`app/import.mts`, `app/ui/import.mjs`): paste what a paste-transport client's scanner printed, import a folder of scan files, or rescan the inbox for a drop a folder watcher missed. `POST /api/import` generalized beyond the one hard-coded `tazuo` adapter; added `POST /api/import/paste` and `POST /api/import/rescan`. A pasted document whose own declared adapter doesn't match the one it's filed under still lands and folds, but returns (and the tab shows) a warning rather than filing it silently.
- Added the Razor Enhanced adapter (`adapters/razor-enhanced/`, Windows-only, folder transport): `packrat-scanner.py` and `packrat-bridge.py`, two IronPython 3.4 scripts written against Razor Enhanced's own published API reference. **Unverified against a live client** (no Windows machine was available during development) — ships without `fixture.scan.json` for exactly that reason, and its README's "Status"/"What's still outstanding" sections say plainly what that means. Razor Enhanced has no fixed install location (its own official docs: "unpack the archive in your own folder, run Razor.exe"), so this adapter has no auto-detected candidate path in the setup wizard — the manual folder picker is the only way in.
- Added the ClassicUO web client adapter (`adapters/classicuo-web/`, paste transport): `packrat-scanner.ts`, one TypeScript script for the web client's scripting panel that prints a scan to its console (compact JSON, chunked into fixed-size lines, wrapped in `-----BEGIN/END PACK RAT SCAN-----` markers) for the player to copy into the Import tab. Also unverified against a live client; also ships without a fixture.
- Generalized the setup wizard and installer to work with any adapter instead of assuming TazUO everywhere: per-adapter candidate client-folder detection (`app/installer.mts`'s `candidateClientRoots`/`NESTED_SCRIPTS_SUFFIX`), a wizard branch for a paste-transport client (nothing to locate or install — sent to the Import tab instead), the bridge's Highlight/Grab/Go-to controls driven by the configured client's own `capabilities.bridge` rather than assuming every client is TazUO, and the wizard/Import tab defaulting to the first *installable* adapter rather than whichever adapter's directory name happens to sort first (never silently landing a new player on a paste-transport client). Dropped Settings' duplicate import panel in favor of the Import tab, which is also where the adapter picker, paste box and rescan button actually live. Added an optional `platform` field to `capabilities.json` (`docs/adapter-guide.md`'s "Platform restriction") for a client that only runs on one OS — Razor Enhanced sets `"platform": "win32"` — and `GET /api/setup` now reports this machine's own `platform` alongside it; the wizard and Import tab still show a platform-incompatible adapter (so a player who's heard of it isn't left wondering why it's missing) but disable choosing it and never default to it, all driven by the adapter's own data rather than a hard-coded adapter id anywhere in the page. Closes Phase 6.
- Renamed every environment variable from its legacy `VAULT_*` form to `PACKRAT_*` (`PACKRAT_DATA`, `PACKRAT_PORT`, `PACKRAT_TOKEN`, `PACKRAT_CORE`) and `QM_FORCE_NO_HIGHS` to `PACKRAT_NO_HIGHS`, across the server, the adapters, the bench harness, the tests and the docs — closing out the last trace of the app's previous names.
- Added `electron-builder` packaging (`package.json`'s `build` block): unsigned mac (`dmg`/`zip`, x64 + arm64), Windows (`nsis`/`portable`, x64) and Linux (`AppImage`, x64) targets, `npm run dist`/`dist:dir`/`dist:publish`, and `scripts/packaging.test.mjs` proving the config (identity, targets, artifact naming, the publish scripts). `build/README.md` documents the still-missing application icon.
- Added a Playwright end-to-end test (`scripts/ui-smoke.test.mjs`, `[slow]`) that drives the real Electron window — title, `#status`, the first-run wizard's Skip path, the demo inventory rendering, a tab switch, no `pageerror` — as the automated stand-in for a full UI test until a human pass. `electron/README.md` and `TESTING.md` updated to describe what it covers versus what still needs a human.
- Added `.github/workflows/ci.yml` (test on macOS, Windows and Linux runners, with `xvfb-run` and an Electron-sandbox workaround on Linux) and `.github/workflows/release.yml` (on a `v*` tag, builds all three platforms and publishes every artifact to a draft GitHub release). `TESTING.md` gained a CI section describing both.
- Scrubbed every private reference out of the tracked tree — the maintainer's name and machine, the private workspace this project grew out of, real in-game character names (replaced with the fictional `Dorran`/`Kestrel`/`Rowan`/`Sable`), and the project's retired product name — and added `scripts/scrub.test.mjs`, a `[smoke]` guard that fails the suite if any of them reappear in a tracked file.
- Added the documents a stranger reads first: a player-facing `README.md` (what the app does, how to install it, how to get past each platform's unsigned-build warning, first run, the game-side scripts, and where your data lives), `PRIVACY.md`, `SECURITY.md`, `CODE_OF_CONDUCT.md`, `RELEASING.md` (the release checklist and the Before 1.0 list), and the GitHub issue/pull-request templates (`.github/ISSUE_TEMPLATE/`, `.github/PULL_REQUEST_TEMPLATE.md`). `CONTRIBUTING.md` now points at all of them.
- Added `package.json` (MIT license, Node engine floor 22.18), an MIT `LICENSE`, and a Node-native test runner (`npm test`, `npm run test:smoke`, `npm run test:fast`) replacing the shell-script runner.
- Added `app/config.mjs`, a single config resolver for the data directory (`--data` flag, then `VAULT_DATA`, then `~/.pack-rat`) and a Node launcher (`npm start`) used by the server, the adapters, and the bench harness alike.
- Pointed the suit-builder scale benchmark at the resolved data directory instead of a hard-coded path.
- Replaced the real-data fixtures with synthetic ones generated from the empirical shape of real gear, under fictional character names (`demo-Kestrel`, `demo-Dorran`), so the repo ships no personal data.
- Settled the app's option and schema contracts: `allowOthersWorn` (not the old flag name), budgets expressed in milliseconds, a `schemaVersion` on profiles and runs, no more `scansDir` option, and RFC 3339 timestamps for the bridge queue/status files and saved runs (scan `scannedAt` stays naive local time, matching the TazUO adapters, until a v2 scan schema revisits it).
- Scrubbed private references (personal paths, character-specific notes) out of the app's working notes and moved the developer-facing content into a root `CONTRIBUTING.md`.
- Renamed the TazUO adapter scripts to `adapters/tazuo/packrat-{scanner,refresh,bridge}.py`, gave them a shared, configurable data-directory helper (`packrat-paths.json` beside the script, else `VAULT_DATA`, else `~/.pack-rat`) matching `app/config.mjs`, and made their file writes atomic.
- Compiled `scripts/optimizer-core.ts` once, via a new `scripts/build-core.mjs` (Node's native type stripping, no external compiler), into a git-ignored `app/dist/optimizer-core.mjs`; the server, both test harnesses, the bench and the MIP spike all now import that one built module (overridable with `VAULT_CORE`) instead of each writing its own throwaway temp-file copy.
- Moved every test onto Node's built-in `node:test`, with a `[smoke]`/`[fast]`/`[slow]` tag prefixed on each test's own name and `scripts/test-runner.mjs` rewritten on the `run()` API to filter by tag and write `test_logs/latest_summary.json`; added `TEST_SKIP_SLOW=1` to skip the exhaustive/exact-search proofs.
- Exported `startServer(config)` from `app/vault-server.mjs` (nothing runs at import time, so a test — or a future desktop shell — can start and stop independent instances), added a `GET /ui/<name>` allowlist route for the page's scripts and styles, put a Content-Security-Policy on every `text/html` response and `x-content-type-options: nosniff` on every response, and added a 415 for any `PUT`/`POST` whose body isn't declared `application/json`; added `app/server.test.mjs` covering the CSP, the `/ui/` allowlist (traversal and unlisted files rejected) and the 415.
- Split the single-page app's inline `<script type="module">` into `app/index.html` (markup + tab nav only) and ten `app/ui/*.mjs` modules plus `app/ui/styles.css`, served through the new `/ui/` allowlist route; dropped the Google Fonts CDN link (no remote loads under the CSP; system fonts already had a real fallback) and the resulting favicon 404.
- Split the saved-runs drawer (history, open, rename, compare) out of `app/ui/builder.mjs` into its own `app/ui/runs.mjs`.
- Added the scan file schema v2 (`app/schema/scan.v2.schema.json`, mirrored byte-for-byte in `app/scan-schema.mjs` for the browser), a zero-dependency validator, and `upgradeScan` — every v1 scan file on disk is upgraded to v2 in memory on read (RFC 3339 timestamps with this machine's DST-correct offset, a stamped `adapter` block, numeric serials throughout, `nameSource` on every item) and validated before it reaches the fold; an invalid or unparsable scan file is logged and skipped rather than taking down the whole inventory. `foldSnapshots` now orders every snapshot by parsed epoch instead of string comparison, and an `opened: false` root is treated exactly like a root the scan never mentioned.
- Moved shard-specific numbers (property caps, the Resisting Spells resist-bonus formula, race cap overrides, tag-penalty units, the rarity ladder, the gargoyle race-lock policy, the free-skill list) out of code and into `app/rules/<id>.json`, validated against `app/schema/rules.v1.schema.json`; added `GET|PUT /api/settings` (the active shard) and `GET /api/rules` (the active shard's rules plus every builtin and user-supplied shard `listRules()` finds), and a header shard picker. `profiles.json` moved to `schemaVersion: 2`, dropping its own `caps` key now that caps live in the rules file (`migrateProfiles` handles the old shape on read).
- Added the bridge protocol v1 schema (`app/schema/bridge.v1.schema.json` — command, result and status, validated separately) and had the TazUO adapter scripts emit scan v2 and speak bridge v1 throughout; added each script's `capabilities.json`-matching `CAPABILITIES` dict, an anonymised `adapters/tazuo/fixture.scan.json` (regenerated from the real scan with the most nested containers), and `app/contracts.test.mjs`, which folds every adapter's fixture against its own `capabilities.json` and the shared schemas with no adapter-specific test code required.
- Added a localhost security layer: Host and Origin checks on every request; an optional per-launch `--token`/`VAULT_TOKEN` bearer check on every `/api/*` route except the SSE progress stream (which instead confirms its `?client=` query parameter against the job's own unguessable id); `app/schema/profiles.v2.schema.json` validation plus a 1 MB cap on `PUT /api/profiles`; one running optimize job per `X-Client-Id` (a second request cancels and supersedes the first); and stack-free 500s — a route or job failure returns a short ref to the client and writes the real stack to `<data>/logs/server.log` keyed by that ref, never to the response body.
- Documented the scan, bridge, and shard rules contracts for third parties (`docs/scan-schema.md`, `docs/bridge-protocol.md`, `docs/shard-rules.md`, `docs/adapter-guide.md`) and closed Phase 2: `POST /api/forget` now refuses with 409 under `--demo` instead of writing a tombstone into the committed `app/fixtures/`; the character sheet's resist caps read the active shard's rules (race-aware, e.g. an Elf's Energy cap) instead of a hardcoded 70; `scripts/optimizer-core.ts` guards a profile with no `weights` key the same way it already guarded `caps`/`floors`; and a shard-swap test now restores the default rules in a `finally` so an assertion failure can't leave later tests running under the wrong shard. Closes Phase 2.
- Replaced the exact search's multi-thread branch-and-bound (a worker pool splitting the tree into tasks over a shared best-score counter, `app/shared-search.mjs`) with [HiGHS](https://highs.dev/) (the `highs` npm package, MIT, compiled to WebAssembly) — the one runtime dependency this app now has. `app/mip.mjs` is the pure MIP builder (one binary per slot/item pair, capped weighted properties as a concave column, hard floors as a row plus a constant bonus, soft floors as indicator + partial-credit columns with a relaxing `u` column for dimensions that can go negative, the weapon/shield mutual-exclusion row); `app/mip-solve.mjs` owns the HiGHS runtime (load, solve, callbacks, no-good cuts, and `gapFromEvents`, the pure absolute-gap formula); `app/exact-solver.mjs` orchestrates one job (the core's heuristic for a fast incumbent, HiGHS for the proof, a hard-floors-conflict retry with every hard floor modelled as soft, optional k-best alternatives via no-good cuts, and always hands the winning suit back to the core for the full report so every result field comes from the same code path). `docs/solver.md` documents the model; `app/bench/mip-spike.mjs` is the spike that led to it, kept as evidence. `app/optimize-worker.mjs` now runs the core's own heuristic (`opts.exact` falsy) or this orchestrator (`opts.exact` true) — one worker per job, since HiGHS explores the tree itself and there is nothing left to split across threads. Progress during the exact phase now carries an incumbent and a bound/gap instead of a bare "explored" fraction; when the WASM fails to load, the job falls back to the heuristic result honestly flagged `solver: "fallback"`, never shown as meeting requirements it didn't check.
- Moved suit-building to a pool per character on the server: `POST /api/optimize` now takes a character name and builds its pools server-side (instead of receiving pools computed in the browser), matching the pools every route already reads from the same fold.
- Added `GET /api/items` — a paged, server-searched and server-sorted item table (`app/item-query.mjs`, the pure filter/sort/page/facet/group logic shared by the browser and the server) plus `GET /api/items/by-serial`, and slimmed `GET /api/inventory` down to facets, worn items and root counts now that the full item table is paged separately.
- Retired the multi-thread branch-and-bound path everywhere it was wired in: `scripts/optimizer-core.ts` no longer takes an `opts.shared`/`OptShared` object or reports `tasks`/`tasksDone` in its progress (the single-thread exact search and dominance pruning stay, as the reference the HiGHS-equivalence tests in `app/solver.test.mjs` compare against); `app/shared-search.mjs` is deleted; `app/bench/run-bench.mjs` no longer escalates a cell to a worker pool once one thread fails to prove it, instead reporting it unproven and running a cheap probe for larger N.
- Added `app/watcher.mts`: one inbox per adapter at `<data>/inbox/<adapter>/`, watched with `fs.watch` and a 300 ms per-filename debounce, normalising an accepted file into `<data>/scans/` under a schema-valid v2 name (idempotent against a stuck duplicate, including across a restart) or, after three failed retries, quarantining it to `<data>/inbox/<adapter>/rejected/` with a reason file beside it. Adapter scripts now write into their inbox instead of straight into `scans/`. Added `GET /api/events` (SSE): a shared stream broadcasting every accept/reject to every connected browser tab, so a scan dropped in game shows up without a manual reload (`app/ui/events.mjs`, with a debounced/coalesced reload for a burst of files).
- Added `app/installer.mts` and the setup-wizard routes it backs: `GET /api/setup` (adapters, candidate client folders, install status), `POST /api/setup/locate`, `POST /api/setup/install` (a running-script guard — 409 while a Legion script looks alive in the client, per the bridge's own `status.json` — then a `.new`-then-rename copy of each script and `packrat-paths.json` written last), `POST /api/import` (copy legacy scans into an adapter's inbox, source never touched), `GET /api/update-check` (GitHub releases, off unless `package.json` names a repo), and `POST /api/host/pick-folder`/`open-path` (folder dialog and open-in-Finder, 501 outside a desktop embedder). `GET|PUT /api/settings` grew `setupDone` and `client` fields.
- Added the first-run setup wizard (`app/ui/wizard.mjs`, four steps: shard — with an AFK-rules notice on shards that need it — client, locate, install) and a Settings tab (`app/ui/settings.mjs`: data/log locations, client status, reinstall, run setup again, import, update check).
- Added the Electron shell (`electron/main.mjs`, `electron/server-entry.mjs`): the server runs as a `utilityProcess` child (so a server crash or a stuck watcher can never take the window down with it) listening on an OS-assigned port; a random per-launch bearer token is set as the child's `VAULT_TOKEN` and attached to every outgoing request to the server's origin via `session.defaultSession.webRequest.onBeforeSendHeaders` — the page itself never sees it. A sandboxed `BrowserWindow` (no preload, `contextIsolation: true`, no `nodeIntegration`) loads the local server; navigation outside its origin is blocked and external links open in the OS browser instead of a second window. Single-instance lock, one automatic restart of a crashed server child, a clean `shutdown` handshake on quit so no `server-entry` process is ever orphaned, and a `--smoke` mode (`scripts/shell-smoke.test.mjs`, `[slow]`, skipped without `electron` installed or with `TEST_SKIP_ELECTRON=1`) that boots the real app, confirms the page rendered, and exits — the automated stand-in for a full end-to-end UI test until Phase 5. `npm run desktop` launches it. Added `docs/architecture.md` describing the process layout, the token flow, the data directory (including the new `inbox/`), the watcher's lifecycle and the installer's guard. Closes Phase 4.
- Renamed the project to **Pack Rat** (the previous name collided with existing UO Alive community tools of the same name): `package.json` name and `productName`, the window/page title, the data directory (now `~/.pack-rat`), the Electron utility-process `serviceName` (now `pack-rat-server`), the GitHub API user-agent string, and the TazUO adapter scripts (now `packrat-{scanner,refresh,bridge}.py`, `packrat-paths.json`, `packrat-paths.example.json`), plus every mention across the docs and tests. Closes Phase 4.5.
