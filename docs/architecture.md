# Architecture: the desktop shell

How the pieces in `electron/`, `app/watcher.mts` and `app/installer.mts` fit together — the processes, the token, the data directory, and what changes (or doesn't) between the desktop app and the bare `npm start` server. Read `CONTRIBUTING.md`'s Files table for the server's own routes and modules; this document is about the shell around it.

## Processes

Three processes make up the running desktop app, plus one worker thread spawned per suit-build job.

`electron/main.mts` is the Electron **main process** — the only code in the app that can touch the OS (native dialogs, opening a path in Finder/Explorer, the single-instance lock). It never runs any of the server's own code.

`electron/server-entry.mts` is a **`utilityProcess.fork()`** child of main: an ordinary Node process, not an Electron renderer, with no access to Electron's own globals. It just calls `app/vault-server.mts`'s `startServer()` — the same function `npm start` uses — with one addition, a `host` object (see The host bridge, below). Running the server here rather than inside main means a server bug, a stuck watcher, or a worker thread pegging a CPU core can never take the window or the OS-facing code down with it, and the server process can be killed and restarted on its own.

The **`BrowserWindow`** is a sandboxed renderer (`sandbox: true`, `contextIsolation: true`, `nodeIntegration: false`, `spellcheck: false`, no preload script) pointed at `http://127.0.0.1:<port>/`. It is a plain browser tab as far as the page's own code is concerned — every `fetch()` and `EventSource` call it makes is the same code that runs under the bare server, unmodified. The guards around it are attached from `app.on("web-contents-created")` so that any future `webContents` inherits them rather than depending on somebody remembering: off-origin navigation refused on `will-navigate`, `will-frame-navigate` and `will-redirect`, `<webview>` attachment refused, `setWindowOpenHandler` denying every new window and handing only a length-bounded, throttled `https:` URL to the OS browser. The session denies every permission request, permission check and device request outright — loopback is a Secure Context, so with no handler Electron would *grant* camera, microphone, geolocation and clipboard to a page that needs none of them. `electron/README.md` has the detail; `docs/threat-model.md` has the reasoning.

Inside the server child, `app/optimize-worker.mts` runs as a **worker thread** per suit-build job (`POST /api/optimize`), same as under the bare server — the shell doesn't change how builds run, only what forks the server that starts them. One job per client (a second request from the same `X-Client-Id` supersedes the first), and at most **four** running server-wide; past that `POST /api/optimize` answers 429 rather than spawning threads on demand. A finished job's result is kept for ten minutes after it finishes and then dropped. A build is never cancelled for running long while it is inside its own time budget (the route accepts up to 60 minutes); only one still running ten minutes past that budget is cancelled as stuck. Quitting the server mid-build ends the build without logging it as a failure.

Requests are not left open indefinitely, which for a server whose exact searches legitimately run for minutes needs stating precisely: `headersTimeout` is 60 seconds (nothing honest takes a minute to finish sending headers), `requestTimeout` is 300 seconds, and only `server.timeout` — the idle-socket one — is disabled, because an SSE stream is an idle socket by design.

## The token

Main generates a random UUID once per launch and hands it to the server child as the `PACKRAT_TOKEN` environment variable when it forks it, which makes `vault-server.mts` require `Authorization: Bearer <token>` on every `/api/*` route (see `CONTRIBUTING.md`'s Security section — this is the same mechanism a developer can turn on by hand with `--token`/`PACKRAT_TOKEN` against the bare server). The token never reaches the page, a URL, or a log line: once the child reports which port it bound (`{type: "listening", port}` over `process.parentPort`), main registers a `session.defaultSession.webRequest.onBeforeSendHeaders` handler scoped to that origin that stamps every outgoing request to it with the header itself. The page's own `fetch()`/`EventSource` calls carry no token at all and need no knowledge that one exists — `GET /api/optimize/<id>/events` is deliberately exempt from the token check for exactly this reason (`EventSource` cannot set custom headers), authenticated instead by its unguessable job id plus a `?client=` match; `GET /api/events` (the inbox-watcher stream, below) is not exempt, since the shell's header attaches to every request to the server's origin regardless of path.

## The host bridge

Two things only the OS can do — show a native folder picker, open a path in Finder/Explorer — are needed by the setup wizard and the Settings tab, but the server has no window and no Electron APIs of its own. `vault-server.mts` accepts an optional `{ host }` with `pickFolder({title}) -> Promise<string|null>` and `openPath(which) -> Promise<void>`, which back `POST /api/host/pick-folder` and `POST /api/host/open-path` (both 501 under the bare server, which passes no `host`). `server-entry.mts` implements `host` by sending `{type: "host", id, op, args}` to main over `process.parentPort` and awaiting the matching `{type: "host-result", id, result}` reply, keyed by `id` so concurrent calls never cross; main does the real `dialog.showOpenDialog`/`shell.openPath` call and posts the result back. This is the only traffic on the parent-child channel besides the startup `listening` message and the `shutdown` message main sends on quit.

**Neither argument crosses that channel unchecked**, because the whole point of the split is that the child is the lower-trust half. `openPath` carries a **discriminator** — the literal `"data"` or `"logs"` — not a path: the route refuses anything else with a 400, and `electron/host-args.mts`'s `openPathTarget` maps it back to one of the two directories main computed for itself, refusing and logging anything it does not recognise. That matters because `shell.openPath` means "open this the way a double-click would", which on every platform can start a program. `dialogTitle` does the same job for the folder picker's caption: strip control characters, trim, cap at 120, and hand back nothing at all for a non-string. A host call that never answers is abandoned after a minute so the HTTP request behind it can return 504 instead of holding a socket forever.

## Data directory layout

One directory holds everything, resolved the same way for the bare server and the shell: `--data <dir>`, else `PACKRAT_DATA`, else (desktop only) `app.getPath("userData")`, else (bare server) `~/.pack-rat`.

```
<data>/
  scans/              normalised, schema-valid v2 scan files — what GET /api/inventory folds
  inbox/<adapter>/     where each adapter drops raw scan files (temp-then-rename); watched
    rejected/          a file that failed to parse/validate after retries, plus a .reason.txt
  profiles.json        per-character suit-builder profiles and templates
  settings.json        {shard, setupDone, client: {adapter, scriptsDir} | null}
  scan-blacklist.json  the containers scans skip: [{serial, name, addedAt, where?}], written by the app and by the TazUO packrat-blacklist.py
  ui-prefs.json        the page's view choices ({cols, colsVersion, colWidths, sheetProps, theme, appearance, sidebar, density}: the Inventory columns and their widths, the character sheet's properties, the look, a pinned-collapsed sidebar)
  rules/                user-defined or overriding shard rules files
  runs/                 one file per finished suit-build job
  bridge/<adapter>/     queue.jsonl (commands) and status.json (the bridge script's heartbeat); the CONFIGURED client's own directory, not a fixed "tazuo"
  logs/
    server.log           the server's own request/error log (ref-keyed stack traces)
    shell.log             the shell's own lifecycle log (desktop only — see Logs, below)
```

`app/config.mts`'s `resolveConfig()`/`ensureLayout()` computes and creates every one of these paths; the shell adds nothing to that function, it just supplies `--data`/`PACKRAT_DATA` before the child ever calls it. Under `--demo`, `scans` points at the committed `app/fixtures/` instead of `<data>/scans`, and no inbox watcher starts at all — the demo fold must never be written to. Legacy scans a player already has on disk from before the inbox existed are not moved automatically; they come in through the Import drawer's Scan files mode, which reads each file in the page and sends it through `POST /api/import/paste` like a pasted scan (validated and written to `inbox/<adapter>/` for that adapter's watcher). There is no route that copies a whole folder server-side any more. The Import tab (`app/ui/import.mts`, Task 1, Phase 6) is the one place in the UI this lives — the Settings tab used to duplicate it with its own adapter-unaware copy (Task 5, Phase 6 removed that and left a pointer instead).

## The inbox watcher's lifecycle

`app/watcher.mts` runs one `startWatcher()` per adapter directory under `adapters/` that ships a `capabilities.json` (today: all three — `tazuo`, `razor-enhanced`, and `classicuo-web`, though the last never sees a folder drop, since its paste transport only ever reaches its inbox via `POST /api/import/paste`), watching `inbox/<adapter>/` non-recursively for `*.json` changes via `fs.watch`, plus one `scanOnce()` sweep at startup so files dropped while the app was closed are picked up immediately.

**Debounce.** A file's change events are collapsed with a 300 ms timer per filename — an adapter script's temp-then-rename write can fire more than one `fs.watch` event for the same drop, and this keeps it to one ingest attempt.

**Ingest.** `ingestFile()` `lstat`s the file *before* opening it: anything that is not a regular file — a symlink, a directory, a device — is refused unread rather than followed, and so is anything larger than `MAX_INBOX_BYTES` (32 MiB, checked off the inode so a padded file never reaches memory). Only then does it read and `JSON.parse` the file, upgrade it (v1 → v2), validate it against the scan schema, and write it into `scans/` under a normalised name (`<slug>-<stamp>.json`, through `app/atomic-write.mts`'s random temp and rename, created `0600`) before removing the inbox copy. A JSON parse failure is reported as one of three fixed reasons rather than V8's own message, which embeds an excerpt of the file's bytes — that string is written into `rejected/<name>.reason.txt` and pushed to the page, and it should not be a channel for the file's contents. It never throws — every failure mode (bad JSON, a failed upgrade, a schema violation, or an I/O error during the write itself) comes back as an ordinary `{ok: false, reason}` so the retry loop always sees ordinary data. It is also idempotent: before writing, it checks whether `scans/` already holds a file under this document's accepted name (with or without a collision suffix) whose own `character`+`scannedAt` match — if so, nothing new is written, which is what makes a file that got stuck in the inbox (e.g. its final cleanup unlink failed once) safe to re-see on a later `scanOnce()`, including after a process restart, since the check reads the scans directory itself rather than anything kept in memory.

**Retry and quarantine.** A file that fails `ingestFile()` is retried up to 3 times, 700 ms apart (a half-written temp-then-rename can look invalid for a moment; a transient write failure such as a full disk counts as a failed attempt the same way). After the last retry it is moved to `inbox/<adapter>/rejected/<name>` with a `<name>.reason.txt` beside it explaining why, and `onRejected` fires so the page can toast it. A duplicate result never fires `onAccepted` — nothing new was ingested.

**Ordering and shutdown.** Every file, whether discovered by a watch event or by `scanOnce()`, is processed through one promise chain, so two files' ingestion never interleaves. `getShard()` is re-read fresh immediately before each `ingestFile()` call (not cached at startup), so a shard switch via `PUT /api/settings` takes effect on the very next file rather than only after a restart. `close()` sets a `closed` flag every await point checks, so a shutdown mid-retry cuts the chain short instead of running past it.

**Self-healing.** The `fs.watch` handle has an `'error'` listener: an error (Windows reports `EPERM` when the watched folder is deleted or moved, and with no listener that would be an uncaught exception taking the server down) is logged and the watch re-armed a moment later. A deleted inbox is recreated by the next `scanOnce()`, which also re-arms the watch, and a watch event arriving after the folder vanished triggers that sweep itself. `scanOnce()` returns whether it could sweep at all, so `POST /api/import/rescan` answers 503 naming the adapters it could not sweep instead of reporting success over a dead watcher. The server starts its watchers only once its port is bound, so a launch that fails with `EADDRINUSE` has not already moved inbox files into `scans/` or left watchers running.

`GET /api/events` is the one shared Server-Sent-Events stream every connected browser tab listens to: `hello` on connect (which adapters are being watched), `inventory` once a file is accepted, `rejected` once one is quarantined, `ping` every 15 s. `app/ui/events.mts` is the page's one listener; a burst of accepted files (an import, or a full scanner run across several chests) coalesces into one page reload via a 400 ms debounce plus an in-flight guard, rather than one reload per file.

## The installer's guard

`POST /api/setup/install {adapter, scriptsDir}` (backing the wizard's Install step and the Settings tab's Reinstall button, folder-transport adapters only — see below) copies an adapter's scripts into the player's chosen scripts folder (TazUO's `LegionScripts/`, Razor Enhanced's `Scripts/`) and writes `packrat-paths.json` there so the scripts know where this app's data directory is. Before touching anything on disk it checks the adapter's own `capabilities.json` `transport` field and refuses with `code: "noInstall"` for a `"paste"`-transport adapter (docs/adapter-guide.md) — there is no scripts folder to write to at all, so the wizard sends that player to the Import tab instead and never reaches this route. For a folder-transport adapter it then checks the bridge's own `status.json` for a script that looks like it's still running in the client (an `alive` timestamp inside the last 30 s, not `stopped: true`) and refuses with a 409 naming the `-stopall` fix if so — overwriting a script file while a Legion script thread is mid-run against it is exactly the "orphaned script thread" trap the TazUO adapter's own README warns about, so the guard runs first, before any file is copied. Every file is then written through one helper, `atomicReplace` — a random `O_EXCL` temp name beside the destination, an `lstat` refusal of any destination that is not either absent or a plain regular file, a rename into place, and cleanup on failure — so a reader never sees a half-written script mid-install, and a symlink already sitting in the chosen folder under one of these published filenames cannot redirect the write somewhere else. The destination folder is one the *player* picked, frequently an unpacked third-party client archive, and an archive is allowed to contain symlinks; that is the whole reason the helper exists rather than a bare `copyFileSync`. `packrat-paths.json` goes through it too, and is written last, once every script is in position, so an interrupted install never leaves a paths file pointing a partial script set at data it doesn't yet match — except that a hand-authored one naming a *different* `dataDir` is kept rather than overwritten (the documented cross-machine Razor Enhanced setup depends on it), and an unreadable one is copied to `packrat-paths.json.bak` first. `app/installer.mts`'s `candidateClientRoots`/`validateScriptsDir` propose and validate each adapter's own folder shape (`NESTED_SCRIPTS_SUFFIX`) rather than one TazUO-shaped guess for every adapter, rejecting relative paths and Windows UNC forms outright; both `POST /api/setup/install` and `PUT /api/settings` run the folder through that validation and persist the *resolved* path, so the two halves of the wizard cannot drift apart.

One known limit, recorded as a ruling in `docs/threat-model.md` rather than fixed: the running-script guard reads the *bridge's* `status.json`, and only the bridge script writes one. The scanner and refresh scripts are overwritten by an install too and publish no liveness, so an install can still rewrite a scanner mid-run. Closing that needs every adapter script to publish a heartbeat — an adapter protocol change.

## Capability-driven bridge controls

The page never hard-codes which client is running. `GET /api/setup` returns `settings.client` (`{adapter, scriptsDir} | null`, which adapter Settings has configured), `adapters` (every `adapters/<id>/`'s own `capabilities.json`, `listAdapters()`'s output, capabilities included), and `bridgeAdapter` (the adapter id `app/vault-server.mts`'s own `bridgeAdapter()` is routing `POST /api/bridge`/`GET /api/bridge/status` to right now — `null` if that id isn't among `adapters` at all) in one response, cached as `state.setup` at page load and refreshed whenever the wizard or the Settings tab changes it. `app/ui/bridge.mts`'s `currentAdapter()` looks `state.setup.settings.client.adapter` up in `state.setup.adapters` to get that adapter's `capabilities.bridge` — the list of actions (`"highlight" | "grab" | "goto"`) its bridge script actually implements — and falls back to looking up `state.setup.bridgeAdapter` instead when no client is configured at all (a player who pressed Skip in the wizard, or installed an adapter's scripts by hand: `settings.client` is unset on purpose in both cases, but a bridge script may well be running and the server is already routing to it). `actButtons()` and `grabAllRow()` only render the buttons that list contains, and `bridgeNote()`/`bridgeNoteEl()` produce the one-line explanation — no client configured and no fallback either, which of the three actions this client's adapter doesn't support, or (new) that the buttons are working against the fallback adapter by default and where to pin down a real one in Settings — that the Inventory tab, the Suit Builder's result panel, and the Settings tab's Client panel each show once rather than leaving a bridge-less player to notice a button is silently missing. Only one client is ever physically connected at a time, so this is deliberately a single global decision — not something recomputed per item from whichever character's scan happened to produce it (an early version did that, which let a bridge-less character's rows still show buttons whenever any other scanned character's adapter had one).

`GET /api/setup` also returns `dataDirCheck` (`app/installer.mts`'s `checkScriptsDataDir`): whether the configured client's scripts folder, or with none configured any auto-detected one holding a Pack Rat script, writes to this data directory. It reads that folder's `packrat-paths.json` through the installer's bounded, no-follow regular-file read, and a missing file (or an empty `dataDir`) counts as the scripts' own default, `~/.pack-rat`. A relative, UNC or device `dataDir` gives no verdict and is never opened. The search is `startServer`'s `clientSearch` option (default: the real home and `candidateClientRoots`), which tests replace so they never reach a real client folder (`startServer`'s `clientSearch` option in-process, `PACKRAT_CLIENT_HOME` for an Electron launch). `"mismatch"` names both folders, `"unreadable"` says why the file couldn't be read; `"match"` and `"none"` say nothing. The server prints the same sentence at startup and Settings › Data shows it in full; the page's banner above every screen says it in one short line (`ui/messages.mts`'s `dataDirBanner`) with "Show details" (to Settings › Data) and Dismiss, and the bridge pill, when offline, says the scripts write to another folder. Nothing is changed or switched: the fix is `--data` or a reinstall. Always `"none"` under `--demo`.

`ADAPTERS_DIR` (`app/config.mts`'s `paths.adaptersDir`, default `adapters/` at the repo root) is resolved through `resolveConfig()` like every other path — overridable with `--adapters <dir>` or `PACKRAT_ADAPTERS_DIR`, the same pattern `PACKRAT_CORE` uses — so a test can start a real server against a throwaway folder of fixture adapters (one with a full bridge, one with none) instead of the repo's real ones; see `app/server.test.mts`'s `GET /api/setup: an adapter with no bridge...` test.

## What the bare server can and cannot do

Everything above except the token, the host bridge, and the two `logs/shell.log`/desktop-specific data-directory defaults applies equally to `node app/vault-server.mts` / `npm start`. The bare server watches the same inbox, runs the same installer routes, and serves the same page — it just has no native folder picker or Finder integration (`POST /api/host/*` answers 501 without an embedder-supplied `host`, so the wizard and Settings tab fall back to a plain text input for a folder path) and, without a `--token`/`PACKRAT_TOKEN` set, runs with no token check on `/api/*` at all. Nothing in `app/` imports from `electron/` — the dependency runs one way, shell to server, which is what keeps `npm start`, `npm test`, and every non-Electron embedding of `startServer()` working unchanged.

## Logs

`<data>/logs/server.log` is the server's own log, unchanged by the shell: an activity and error log, not a request log — successful requests are never logged. It carries watcher accepts and rejects (by absolute path, so it names the player's folder layout and their character names), install progress, and ref-keyed stack traces for a route or job failure (see `CONTRIBUTING.md`'s Security section), never returned to the client itself. `<data>/logs/shell.log` is desktop-only, written by `main.mts`: its own lifecycle lines (`shell: starting …`, `shell: quitting …`) plus everything the server child prints to stdout/stderr, prefixed `server: `. A player reporting a bug needs to attach both files, or the ref from `server.log` alone if that's all that's relevant — and `PRIVACY.md` warns them what attaching one discloses.

The token appears in neither: the server prints `token: set` or `token: none (dev)` and never the value, and the 500 handler logs a request's method and pathname but not its query string or headers. Neither file is rotated or size-capped; both grow for the life of the install and are safe to delete at any point. `server.log` is written owner-only (`0600`); the shell creates the `logs/` directory `0700` and `shell.log` `0600` when it first makes them, so neither is readable by another account on a shared machine.

## Packaging

`package.json`'s `build` block is treated as product interface — `scripts/packaging.test.mts` asserts its shape — and two parts of it matter here.

**Fuses.** A packaged build flips the Electron fuses that would otherwise leave the installed binary usable as a general-purpose Node interpreter: `runAsNode`, `enableNodeOptionsEnvironmentVariable`, `enableNodeCliInspectArguments` and `grantFileProtocolExtraPrivileges` off, `onlyLoadAppFromAsar` on, and `resetAdHocDarwinSignature` on because flipping a fuse rewrites the binary and invalidates the ad-hoc signature it shipped with. `enableEmbeddedAsarIntegrityValidation` and `enableCookieEncryption` are deliberately left unset, each for a stated reason — `electron/README.md` and `docs/threat-model.md` both carry it. A practical consequence: `--inspect` does nothing in a packaged app, so anything that drives Electron over the Node inspector can only drive a dev checkout; use `--smoke` against a packaged build instead. None of this affects a dev checkout, where fuses are never flipped.

**What is unpacked.** `asar: true`, and `asarUnpack` is exactly `["adapters/**", "node_modules/highs/**"]` — nothing else. The adapters must be real files because the installer `copyFileSync`s the `.py` out of them into the player's client folder, and HiGHS ships a `.wasm` that is read from disk. Everything the app actually *runs* — the server, the config resolver, the installer, the watcher, the compiled page, and `scripts/optimizer-core.mts` — stays inside `app.asar`, which Electron's fs and module patches serve fine, worker threads and a dynamic `import()` of a `.mts` file included. The `--smoke` run's third check exists to prove exactly that end to end: it starts a real optimize job from the renderer, which is the one path that exercises a worker thread loading out of the asar and HiGHS loading out of `app.asar.unpacked` beside it.
