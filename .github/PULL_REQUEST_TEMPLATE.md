## What changed and why

## Tests

Which tests cover this change — new ones you added, or existing ones that already exercised this path? Note the tag (`[smoke]`/`[fast]`/`[slow]`) on anything you added, per `TESTING.md`.

## Checklist

- [ ] `npm test` passes.
